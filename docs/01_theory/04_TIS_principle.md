# Temporal Interference Stimulation (TIS) 원리

## 학습 목표

TIS의 물리적 원리를 파동 간섭 → 전기장 수식 → COMSOL 시뮬레이션 워크플로우까지 연속된 흐름으로 이해하고,  
2-phase TIS와 3-phase TIS의 차이를 AM_max 계산 관점에서 설명할 수 있다.

---

## 전체 그림

```
[문제: 깊은 신경을 비침습적으로 자극할 수 없다]
         ↓
  두 개의 고주파 전류(f1, f2)를 피부 전극으로 인가
         ↓
  조직 내부에서 두 전기장이 중첩(superposition)
         ↓
  공간적으로 겹치는 영역에서만 "맥놀이(beat)" 발생
         ↓
  저주파 envelope(f_beat = |f1 - f2|)이 신경 발화 유도
         ↓
[피부·표면 조직: kHz 고주파 → 자극 없음]
[내부 표적 지점: 저주파 envelope → 신경 발화]

핵심 통찰: "자극은 envelope이 한다. kHz 자체는 통과할 뿐."
```

---

## 1. TIS 탄생 배경

### 왜 기존 방법은 실패했는가?

```
기존 경두개 전기자극(tDCS/tACS)의 딜레마:

  피부 전극 ──────────────────────── 두피/두개골
       │  전류가 표면을 따라 흐름     │
       │  내부는 낮은 전류밀도       │
       └─────────────── 표적(내부) ──┘
                           ↑
               여기까지 충분한 세기로 도달하려면
               표면에서 너무 강해져 통증·화상 유발

물리적 이유:
  - 두개골 conductivity ≈ 0.01 S/m (근육의 1/40)
  - 대부분의 전류가 두개골을 우회하여 표면 경로 이동
  - 내부 전류밀도 ∝ 1/r² (거리 제곱에 반비례 감소)
```

### Grossman et al. 2017 (Cell) — TIS 제안

- **논문**: Noninvasive Deep Brain Stimulation via Temporally Interfering Electric Fields
- **핵심 아이디어**: 주파수가 *조금* 다른 두 kHz 전류를 따로 인가
  → 표면에서는 고주파만 존재 (신경 자극 없음)
  → 내부 표적에서 두 전기장이 중첩 → 저주파 envelope 생성 → 신경 발화
- **동물 실험 결과**: 마취된 마우스에서 운동피질·해마 선택적 자극 확인

```
Grossman 2017 실험 구성:

  ┌─────────────────────────────────────┐
  │           마우스 두뇌               │
  │                                     │
  │   전극쌍 1 ──f1=2000Hz──┐          │
  │                          ├→ 표적 ←─┤
  │   전극쌍 2 ──f2=2001Hz──┘          │
  │                                     │
  │   결과: 표적에서 1Hz 자극 envelope  │
  │         발에서 움직임 관찰          │
  └─────────────────────────────────────┘

참조: Grossman N et al., Cell 2017; 169(6):1029–1041
```

---

## 2. 물리적 직관: 맥놀이(Beat) 현상

### 소리로 먼저 이해하기

피아노에서 라(A) 음을 440Hz와 442Hz로 동시에 치면:
→ "웅~웅~" 하는 2Hz의 떨림(맥놀이) 들림
→ 두 음파의 주파수 차이 = 맥놀이 주파수

TIS는 이와 동일한 원리를 전기장에 적용한 것.

```
맥놀이 시각화:

  파동 1 (f1=2000Hz):   ∿∿∿∿∿∿∿∿∿∿∿∿∿∿∿∿∿∿∿∿
  파동 2 (f2=2002Hz):   ∿∿∿∿∿∿∿∿∿∿∿∿∿∿∿∿∿∿∿∿
  
  중첩 결과:
                         ╭──╮        ╭──╮
  ∿∿∿∿╭∿∿∿∿∿∿∿╮∿∿∿∿╭∿∿∿∿∿∿∿╮∿∿∿∿
       ╰─────╯        ╰─────╯
  
  ↑                    ↑
  진폭 최소             진폭 최대
  (소상: destructive)   (보강: constructive)
  
  맥놀이 주파수 = |2002 - 2000| = 2Hz
  고주파 carrier = (2002 + 2000)/2 = 2001Hz
```

---

## 3. 수학적 표현

### 3.1 1D 스칼라 경우 (기초)

두 전기장 성분이 같은 방향으로 중첩될 때:

$$E(t) = E_1 \cos(\omega_1 t) + E_2 \cos(\omega_2 t)$$

여기서 $\omega_1 = 2\pi f_1$, $\omega_2 = 2\pi f_2$

**$E_1 = E_2 = E_0$인 특수한 경우** (두 쌍의 진폭이 같을 때):

적화공식(sum-to-product) 적용:

$$E(t) = 2E_0 \cos\!\left(\frac{\omega_1 - \omega_2}{2}t\right)\cos\!\left(\frac{\omega_1 + \omega_2}{2}t\right)$$

$$= \underbrace{2E_0 \cos(\pi\Delta f \cdot t)}_{\text{느린 envelope}}\underbrace{\cos(2\pi \bar{f} \cdot t)}_{\text{빠른 carrier}}$$

| 항 | 의미 | 주파수 |
|----|------|--------|
| $\cos(2\pi\bar{f}t)$ | 고주파 carrier | $\bar{f} = (f_1+f_2)/2$ ≈ 수kHz |
| $2E_0\cos(\pi\Delta f \cdot t)$ | 저주파 envelope | $\Delta f = |f_1 - f_2|$ (신경 자극) |

**Envelope 진폭** = $|2E_0 \cos(\pi\Delta f \cdot t)|$  
→ 주기: $1/\Delta f$ 초 (맥놀이 1회 = 신경 1회 자극)

> **팀 실험 파라미터 (하이퍼틱스, 2026):**  
> f1 = 5000Hz, f2 = 5002Hz → $\Delta f$ = **2Hz** beat, carrier ≈ 5001Hz  
> 진폭: 1.6mA/채널 → 쥐 운동피질에서 spike firing rate 증가 확인

### 3.2 $E_1 \neq E_2$인 일반적 경우

진폭이 다를 때 envelope은 완전히 0까지 내려가지 않음:

$$E_{env}(t) = \sqrt{(E_1 + E_2)^2\cos^2(\pi\Delta f \cdot t) + (E_1 - E_2)^2\sin^2(\pi\Delta f \cdot t)}$$

- **최대 envelope 진폭** = $E_1 + E_2$ (보강 간섭 시)
- **최소 envelope 진폭** = $|E_1 - E_2|$ (소상 간섭 시)
- **AM 깊이** = $2\min(E_1, E_2)$ (진폭 변조의 크기)

### 3.3 3D 벡터 전기장 (실제 조직에서)

실제 조직에서 전기장은 3차원 벡터:

$$\vec{E}(t) = \vec{E}_1\cos(\omega_1 t + \phi_1) + \vec{E}_2\cos(\omega_2 t + \phi_2)$$

여기서 $\vec{E}_1$, $\vec{E}_2$는 각각 전극 쌍 1, 2가 만드는 공간 전기장 벡터.

**핵심**: 공간의 각 점에서 $\vec{E}(t)$는 시간에 따라 **타원 궤적**을 그린다.

---

## 4. 전기장 벡터의 타원 궤적 (Elliptical Trajectory)

```
공간의 한 점에서 전기장 벡터 끝이 그리는 궤적:

     E_y
      ↑
      │    ╭────────╮
      │   ╱  타원   ╲
      │  │     ●     │  ← 전기장 벡터 E(t)가
      │  │           │     회전하며 타원을 그림
      │   ╲         ╱
      │    ╰────────╯
      └──────────────→ E_x

  - 장반경(semi-major axis) a: 가장 강한 자극 방향
  - 단반경(semi-minor axis) b: 수직 방향 자극
  - b = 0이면 선형(linear) → 특정 방향만 자극
  - a = b이면 원형(circular)

2-phase TIS: 타원이 한 평면 내에 제한됨
3-phase TIS: 타원이 3D 공간에서 임의 방향 가능
```

**Phasor 표현**으로 타원 기술:

COMSOL에서 export하는 복소수 phasor $\tilde{E}$를 이용:

$$\vec{E}_1 = \text{Re}[\tilde{\vec{E}}_1 e^{j\omega_1 t}], \quad \vec{E}_2 = \text{Re}[\tilde{\vec{E}}_2 e^{j\omega_2 t}]$$

타원의 두 축은 행렬 $[\tilde{\vec{E}}_1, \tilde{\vec{E}}_2]$의 **특이값(singular value)**으로 결정됨.

---

## 5. AM_max 계산 — 어느 방향으로 가장 강하게 자극되는가?

### 5.1 단일 방향 AM (Single-directed AM)

특정 방향 단위벡터 $\hat{n}$으로의 전기장 성분:

$$E_{\hat{n}}(t) = \vec{E}(t) \cdot \hat{n} = E_{1n}\cos(\omega_1 t) + E_{2n}\cos(\omega_2 t)$$

이 방향으로의 AM 진폭 = 위 1D 공식 적용.

```
COMSOL에서 2-phase 단일 방향 시뮬레이션 (김대용 팀, 2025):

  x방향 AM:  E_x(t) = Ex1·cos(ω1t) + Ex2·cos(ω2t) → AM_x
  y방향 AM:  E_y(t) = Ey1·cos(ω1t) + Ey2·cos(ω2t) → AM_y
  
  → 그림: 타원 투영으로 보이는 AM 분포 맵
```

### 5.2 AM_max — 최대 자극 방향 탐색

**문제**: 어느 방향 $\hat{n}^*$이 AM을 최대화하는가?

$$AM_{max} = \max_{\|\hat{n}\|=1} AM(\hat{n})$$

이것이 팀 연구의 핵심 계산 문제.

**해석적 풀이 (송솔웅, 2026 — Lagrange 승수법)**:

타원 궤적의 각 점 $P(t)$를 단위 구(unit sphere)에 투영:

**제약 조건**:
1. $\|\hat{n}\| = 1$ (단위벡터)
2. $\hat{n}$이 타원 궤적의 특정 점과 접촉 (최대 투영 조건)

**Lagrangian** 구성:

$$\mathcal{L}(\hat{n}, \lambda_1, \lambda_2) = (\vec{E}(t) \cdot \hat{n}) - \lambda_1(\|\hat{n}\|^2 - 1) - \lambda_2 \cdot g(\hat{n})$$

**Stationarity 조건** ($\nabla_{\hat{n}}\mathcal{L} = 0$)에서:

$$AM_{max} = \sigma_1$$

여기서 $\sigma_1$은 **타원 field tensor의 최대 특이값(largest singular value)**.

```
AM_max 계산 요약:

  입력: 각 점에서 E_phasor_f1 = (Ex1, Ey1, Ez1) (complex)
                  E_phasor_f2 = (Ex2, Ey2, Ez2) (complex)
  
  행렬 구성: M = [Re(Ẽ₁), Im(Ẽ₁), Re(Ẽ₂), Im(Ẽ₂)] ∈ ℝ³ˣ⁴
  
  SVD 분해: M = UΣVᵀ
  
  AM_max = 2 × σ₁  (최대 특이값 × 2, 피크-투-피크)
  
  최적 방향: û* = u₁ (U의 첫 번째 열)
```

> **왜 인수 2인가?**  
> 타원의 최대 투영 = 장반경 $a$, 피크-투-피크 진폭 = $2a = 2\sigma_1$

---

## 6. 안티페이즈(Anti-phase) 전극 구성 원리

### 기본 개념: 왜 안티페이즈가 필요한가?

```
❌ 안티페이즈 없는 경우 (단순 2전극):

  (+)전극 ──────────────── (-)GND
       │  전류가 표면 경로  │
       │  (전극 바로 밑)   │
       │  집중됨           │
       └───────────────────┘
  
  문제: 전극 근처 과도한 전류 밀도 → 통증, 화상

✅ 안티페이즈 적용 (2전극 쌍):

  전극쌍 1:  (+f1)──────────(- f1)  ← f1 고주파, 안티페이즈
  전극쌍 2:  (+f2)──────────(- f2)  ← f2 고주파, 안티페이즈

  각 쌍의 전류 합 = 0 → 외부 유출 없음
  내부 교차 영역에서만 envelope 생성
```

**수학적 설명**:

```
표면 근처 임의 점 P에서:

  E₁ = +A·cos(ω₁t)   (전극1에서 오는 성분)
  E₁' = -A·cos(ω₁t)  (안티페이즈 전극에서 오는 성분, 반대 위상)

  합 = A·cos(ω₁t) + (-A·cos(ω₁t)) = 0  ← 표면 전류 소거

  단, 교차 영역 내부에서는:
  E₁(at target) ≠ -E₁'(at target)  ← 경로 차이로 소거 불완전
  → 유효 자극 생성
```

**팀 COMSOL 관찰 (김대용, 2025.10)**:

```
GND 설정에 따른 차이:

  Cross-talk 유발 구성:
  ┌─────────────────────┐
  │  (+f1)   (GND)      │  ← GND 공유 시
  │          ↑           │    다른 주파수 전류가 GND 통해
  │  (+f2) ──┘          │    혼선(cross-talk) 발생
  └─────────────────────┘

  Anti-phase 구성 (권장):
  ┌─────────────────────┐
  │  (+f1)   (-f1)      │  ← 각 쌍 독립 회로
  │                     │    GND 없이 차동 구동
  │  (+f2)   (-f2)      │    → cross-talk 방지
  └─────────────────────┘
```

**실험 파라미터 (Kwak et al., Brain Stimulation, 2023)**:
- f1 = 2000Hz (pair 1: 0.95mA), f2 = 2002Hz (pair 2: 1.05mA)
- Beat frequency = 2Hz, 진폭 비대칭 → steering 효과
- 타겟: 마우스 선조체(striatum)

---

## 7. COMSOL 시뮬레이션 워크플로우

팀이 실제 사용하는 파이프라인 (송솔웅 + 김대용, 2025–2026):

```
┌──────────────────────────────────────────────────────────┐
│                  TIS COMSOL 파이프라인                    │
│                                                          │
│  STEP 1: 조직 모델 구축                                   │
│  ─────────────────────                                   │
│  MRI/CT 영상                                             │
│    → 세그멘테이션 (피부, 지방, 근육, 신경, 뼈)           │
│    → STL/mesh 생성                                       │
│    → COMSOL import                                       │
│                                                          │
│  팀 활용 데이터:                                         │
│  - IT'IS MIDA (115 구조체, 설하신경 포함)                │
│  - Z-anatomy (신경계 전체, atlas 기반)                   │
│  - IT'IS Jeduk v4.0 (전신 인체 모델, COMSOL 미지원)      │
│                                                          │
│  STEP 2: 물질 속성 설정                                  │
│  ──────────────────────                                  │
│  각 조직에 전기 전도도(σ) 및 유전율(ε) 할당:             │
│                                                          │
│    피부: σ ≈ 0.4–1.0 S/m                                │
│    지방: σ ≈ 0.04 S/m                                   │
│    근육: σ ≈ 0.2–0.4 S/m (이방성 고려)                  │
│    신경: σ ≈ 0.1–0.3 S/m                                │
│    뼈:   σ ≈ 0.02 S/m                                   │
│    혈액: σ ≈ 0.7 S/m                                    │
│                                                          │
│  STEP 3: 주파수 도메인 해석                               │
│  ──────────────────────────                              │
│  Physics: AC/DC → Electric Currents                      │
│  Study: Frequency Domain (f1 따로, f2 따로 — 2번 계산)  │
│                                                          │
│  지배 방정식 (Laplace):                                  │
│  ∇ · (σ∇V) = 0  (quasi-static 근사)                    │
│                                                          │
│  경계 조건:                                              │
│  - 전극: 전류밀도 J 인가 (Current source 권장)           │
│  - 외부 경계: 절연(insulation, n·J=0)                   │
│                                                          │
│  ※ Current source vs Voltage source:                    │
│  Current source → 전류 일정, 전극 임피던스 변화에 강건   │
│  (팀은 Current source 사용)                              │
│                                                          │
│  STEP 4: 전기장 데이터 export                            │
│  ─────────────────────────                               │
│  각 주파수(f1, f2)별 복소 phasor export:                 │
│    txt 파일: x, y, z 좌표 + Ex, Ey, Ez (복소수)         │
│    → 파일 1: E_phasor_f1.txt                            │
│    → 파일 2: E_phasor_f2.txt                            │
│                                                          │
│  STEP 5: MATLAB에서 AM_max 계산                          │
│  ─────────────────────────────                           │
│  스크립트: nTIS_3D_260331.m (송솔웅)                     │
│                                                          │
│    for each point:                                       │
│      M = [Re(Ẽ₁), Im(Ẽ₁), Re(Ẽ₂), Im(Ẽ₂)]            │
│      [U,S,V] = svd(M)                                   │
│      AM_max = 2 * S(1,1)                                │
│      n_optimal = U(:,1)                                  │
│                                                          │
│  시각화:                                                 │
│  - Scatter plot (3D 분포)                               │
│  - Slice (단면도)                                        │
│  - Isosurface (등값면)                                   │
│                                                          │
│  STEP 6: COMSOL 재import 및 시각화                       │
│  ──────────────────────────────────                      │
│  Global Definitions → Interpolation 함수 생성            │
│  AM_max 값을 공간 함수로 load                            │
│  → 조직 모델 위에 겹쳐서 시각화                          │
└──────────────────────────────────────────────────────────┘
```

### 경계 Artifact 문제 및 해결 (김대용, 2025.09–10)

```
문제:
  전극 표면 근처에서 AM_max 계산 시 비현실적 큰 값 출현
  
  원인: Case-based AM 계산에서 경계 조건 불연속
  
        Ground truth    Result (artifact)
        ──────────      ─────────────────
        |          |    |    ████        |
        |   균일   |    |   ██  ████    |  ← 경계에서
        |   분포   |    |               |     튀는 값
        |__________|    |_______________|
  
해결책: Binary Mask via Boundary
  
  1. 조직 경계면 추출
  2. 전극 표면 근방 영역 mask 처리 (제외)
  3. AM 계산 결과에 mask 적용
  
  → 경계 artifact 제거 후 ground truth와 일치
```

---

## 8. 2-phase TIS vs 3-phase TIS 비교

### 8.1 2-phase TIS (기본형)

```
전극 구성:

  (+f1) ──────── (-f1)    ← 전극쌍 1 (x방향)
                  
  (+f2) ──────── (-f2)    ← 전극쌍 2 (y방향)

Phasor 관점:
  
  허수축
    ↑
    │    ● E₂
    │   /
    │  /
    │ /    ← 타원이 x-y 평면 내에만 있음
    │/
    ●────────→ 실수축
   E₁

자극 형태:
  - 타원이 2D 평면(자극 평면) 내에 제한
  - Steering: 두 쌍의 진폭 비율 조절 → 좌/우 이동
  - 제한: 상/하(깊이 방향) 조절 자유도 없음
```

**COMSOL 결과 (2-phase, 팀 시뮬레이션)**:
- 중심 영역: 타원 궤적이 선형에 가까움 (steering 가능)
- 전극 근처: 타원이 크게 찌그러짐

### 8.2 3-phase TIS (팀 핵심 연구)

```
전극 구성 (120° 위상 분리):

  전극 a:  I·cos(ωt + 0°)
  전극 b:  I·cos(ωt + 120°)
  전극 c:  I·cos(ωt + 240°)
  
  합: Ia + Ib + Ic = I[cos(0) + cos(120°) + cos(240°)] = 0
                     → Kirchhoff 전류법칙 자동 만족

3-phase의 Phasor 표현:

  허수축
    ↑   ● b (120°)
    │  ╱
    │ ╱
    │╱
  ──●──────────→ 실수축
  a │╲
    │ ╲
    │  ╲● c (240°)

대칭적 3-상 → 전기장 벡터가 3D 공간에서 타원 그림
```

**3-phase vs 2-phase 비교**:

| 항목 | 2-phase TIS | 3-phase TIS |
|------|-------------|-------------|
| 전극 수 | 4개 (2쌍) | 3개 |
| 자극 평면 | 2D (평면적) | 3D (입체적) |
| Steering 방향 | 좌/우만 | 상/하/좌/우/앞/뒤 |
| AM_max 계산 | 상대적 단순 | SVD 필요 |
| 타원 방향 | 전극 배치 고정 | 전류 비율로 조절 |

```
김대용 팀 COMSOL 결과 비교 (251014):

  중심 영역 전기장 벡터 궤적:
  
  2-phase:              3-phase:
  ─────────             ─────────
  E_y ↑                E_y ↑
      │  ─              │   ╭──╮
      │ ╱               │  ╱    ╲
      │╱                │ │  ●  │
      ●─────→ E_x       │  ╲    ╱
                         │   ╰──╯
                         └────────→ E_x
  선형에 가까움           원형에 가까움
  (한 방향 집중)          (모든 방향 균등)
```

---

## 9. 자극 조향 (Steering)

### 진폭 비율 조절로 표적 이동

```
2-phase TIS 조향 원리:

  I₁ = I₀ + ΔI (전극쌍 1의 진폭)
  I₂ = I₀ - ΔI (전극쌍 2의 진폭)

  ΔI = 0:        표적이 중앙
  ΔI > 0:        표적이 전극쌍 1 쪽으로 이동
  ΔI < 0:        표적이 전극쌍 2 쪽으로 이동

시각화:

  ΔI < 0    ΔI = 0    ΔI > 0
  ┌──────┐  ┌──────┐  ┌──────┐
  │  ●   │  │  ●   │  │  ●   │  ← 전극쌍 2
  │ ●    │  │  ●   │  │   ●  │  ← 자극 표적
  │●     │  │  ●   │  │    ● │  ← 이동!
  │  ●   │  │  ●   │  │  ●   │  ← 전극쌍 1
  └──────┘  └──────┘  └──────┘
```

**3-phase TIS 조향 (팀 연구, 하이퍼틱스 2026)**:

```
3개 전류의 크기/위상 조합으로:

  상하 조향:   ↑↓
  좌우 조향:   ←→
  사선 조향:   ↗↙
  
  ← 전극 이동 없이 소프트웨어로 조향!

응용 (경골신경 OAB 치료):
  - 경골신경 단면 내에서 표적 섬유(방광지배) 선택적 활성화 가능
  - 발목 전극 고정 → 전류 파라미터만 변경으로 최적 자극 탐색
```

---

## 10. 팀 COMSOL 시뮬레이션 결과 요약

### 10.1 설하신경(Hypoglossal Nerve) 모델 — 검증 케이스

설하신경 표적 TIS 시뮬레이션은 경골신경 연구의 선행 검증 케이스.

**전극 파라미터 (김대용 팀 260323)**:
- 전극 직경: 1mm (추정값)
- 전극간 거리: 2cm (center-to-center, 정사각형 배치)
- 주파수: f1 = 5kHz, f2 = 5.05kHz (beat = 50Hz) 또는 f1 = 6kHz, f2 = 6.05kHz
- 전류: 남성 7.86±1.46mA, 여성 4.6±1.34mA

**3-TIS AM_max 결과**:

```
Configuration 1 (최대점):
  AM_max = 2.401×10⁴ V/m
  위치: (-0.1, -0.1, -0.0009544) m  ← 전극 근처 표면
  
Configuration 2 (최대점):
  AM_max = 1.610×10⁴ V/m
  위치: (0.1, -0.1, 0.001089) m

주의: 최대값은 전극 직근에서 나타남 → 내부 표적에서의 값은 이보다 낮음
실제 신경에서의 유효 자극 역치: ~10–100 V/m (문헌 기반)
```

### 10.2 전극 최적화 결과 (김대용 팀 260420)

```
최적화 전략:
  후보 전극 위치: 17,290개 조합 탐색
  목적함수: 신경(Nerve) AM_max 최대화
  제약 조건: Off-target AM_max 최소화 (선택성)

  초기 결과:
    Nerve max: 1.04 V/m  vs  Off-target: 0.99 V/m
    → 선택성 불량 (신경/비표적 ≈ 1.05)
  
  Lead-field 기반 Pareto 최적화 후:
    Nerve max: 3.35 V/m  vs  Off-target: 3.34 V/m
    → 선택성 개선 필요 (추가 연구 진행 중)
  
  방법: 4×9 전극 어레이 → Lead-field matrix → 다목적 최적화
```

---

## 11. 신경이 왜 kHz는 무시하고 envelope에만 반응하는가?

```
신경 발화 필터링:

  고주파(kHz) → 신경막 무반응:
  
    신경막의 RC 시상수: τ = RC ≈ 1–10ms
    kHz 신호 주기: T = 1/2000Hz = 0.5ms << τ
    → 막전위가 충분히 변하기 전에 역전 → 순 효과 없음

  저주파(beat) → 신경막 반응:
  
    Beat 주파수: 1–200Hz (신경 발화 범위)
    Beat 주기: T = 1/2Hz = 500ms >> τ
    → 막전위가 서서히 변화 → 역치 초과 → 발화

물리적 비유:
  배를 흔들려면 파도 주기가 배의 자연주기와 맞아야 함.
  너무 빠른 잔물결(kHz)은 배(신경)를 흔들지 못함.
  천천히 오는 너울(Hz)이 배를 흔든다.

참조: Hutcheon B & Yarom Y, Trends Neurosciences, 2000
      (뉴런의 공명 주파수 및 내재 주파수 선호도)
```

---

## 12. 개념 요약

```
TIS 핵심 개념 트리:

TIS
├── 원리
│   ├── 두 kHz 전류 → beat frequency → 신경 자극
│   ├── 고주파: 신경막 통과, 자극 없음
│   └── 저주파 envelope: 막전위 변화 → 발화
│
├── 수학
│   ├── 1D: E(t) = E₁cos(ω₁t) + E₂cos(ω₂t)
│   ├── Envelope = |2E₀cos(π·Δf·t)| (E₁=E₂일 때)
│   └── 3D: 전기장 벡터 → 타원 궤적 → SVD로 AM_max
│
├── 전극 구성
│   ├── 안티페이즈: 표면 전류 소거, 내부 간섭
│   ├── 2-phase: 4전극, 2D 조향
│   └── 3-phase: 3전극, 3D 조향, 자유도 향상
│
└── COMSOL 파이프라인
    ├── 조직 모델 → 물질 속성 → frequency domain 해석
    ├── phasor export (f1, f2 각각)
    ├── MATLAB SVD → AM_max
    └── COMSOL 재import → 시각화
```

---

## 13. 지식 맵: TIS가 OAB 치료에 연결되는 방식

```
[OAB 병태생리 — STEP 1]
         ↓ 방광 과활성 = 신경조절 문제
[경골신경 해부 — STEP 2]
         ↓ S2-S4 천수 반사궁 억제 경로
[PTNS/TTNS/SNM 한계 — STEP 3]
         ↓ 비침습 + 깊은 표적화 필요
[TIS 원리 — STEP 4]  ← 현재 위치
         ↓ 비침습 심부 자극 가능
[Multi-source TIS — STEP 5]
         ↓ 전극 수 증가로 성능 향상
[3-phase TIS — STEP 6]  ← 팀의 핵심 연구
         ↓ 3D 조향 + 경골신경 표적
[COMSOL 최적화 — STEP 7–8]
         ↓ 최적 전극 배치·전류 파라미터
[임상 중개 — STEP 10]
```

---

## 14. 연구자 노트

> **확립된 지식** (논문에서 검증됨):
> - TIS로 마우스 운동피질·해마 선택적 자극 가능 (Grossman 2017, Cell)
> - kHz 고주파는 신경 자극 없이 조직 통과 (membrane time constant 원리)
> - Anti-phase 전극 구성이 표면 자극 최소화에 효과적 (하이퍼틱스 팀 확인)
> - 3-phase TIS의 3D 조향 수학적 원리 증명 (팀 내부 발표, 2025–2026)

> **아직 불확실한 것** (연구 갭):
> - 경골신경에서 TIS envelope이 실제 신경 발화를 유도하는 최소 역치 (in vivo 미확인)
> - 3-phase TIS의 조향 정밀도가 경골신경 단면(직경 ~3–5mm)에서 충분한지
> - 피하 지방층 두께 개인차가 AM_max 분포에 미치는 영향
> - 만성 OAB 환자에서의 반복 자극 효과 및 안전성

> **팀 특이 사항**:
> - 송솔웅의 Lagrange multiplier 기반 해석적 AM_max 풀이는 시뮬레이션 속도를 크게 향상
> - 김대용의 boundary mask 솔루션은 COMSOL 결과 신뢰도 향상에 핵심 기여
> - 전극 파라미터 (직경 1mm, 간격 2cm)는 추정값 — 경골신경 특화 최적화 필요

---

## 15. 핵심 논문 목록

| 논문 | 핵심 내용 | 관련성 |
|------|-----------|--------|
| Grossman N et al., Cell 2017 | TIS 개념 최초 제안, 마우스 DBS 실증 | TIS 원리 기반 |
| Hutcheon B & Yarom Y, Trends Neurosci 2000 | 뉴런 공명주파수, 내재 주파수 선호도 | kHz 무반응 근거 |
| Conta J et al., Sci Rep 2021 | 인체 tTIS 개인차 전기장 분포 | 인체 적용 가변성 |
| Kwak Y et al., Brain Stimulation 2023 | TIS로 선조체 도파민 조절 | 말초 적용 전기화학 |

---

## 16. 자기평가 문항

**Level 1 (최소 이해)**
1. TIS에서 신경을 실제로 자극하는 것은 kHz 고주파인가, beat frequency인가? 이유는?
2. f1 = 3000Hz, f2 = 3005Hz일 때 beat frequency는 얼마인가?

**Level 2 (부분 이해)**
3. $E_1 = E_2 = E_0$일 때 TIS envelope 공식을 유도하라. 어떤 삼각 공식을 사용하는가?
4. 안티페이즈 전극 구성이 표면 자극을 줄이는 이유를 수학적으로 설명하라.

**Level 3 (실용적 이해)**
5. COMSOL에서 2-phase TIS 시뮬레이션 시, 왜 f1과 f2를 따로 두 번 계산해야 하는가?
6. AM_max와 단일 방향 AM의 차이를 설명하고, 언제 AM_max가 더 중요한가?

**Level 4 (연구 준비 완료)**
7. 3-phase TIS에서 전류의 합이 0이 되는 이유를 phasor로 설명하라.
8. SVD를 이용한 AM_max 계산에서 최대 특이값이 AM_max와 같은 이유를 증명하라.
9. 경골신경 TIS 시뮬레이션 설계 시 어떤 전기적 물성값이 가장 중요하고, 그 불확실성을 어떻게 다룰 것인가?

**Level 5 (전문가 수준)**
10. 팀 COMSOL 결과에서 전극 최적화 후에도 신경/비표적 선택성이 개선되지 않은 이유를 분석하고, 개선 전략을 제안하라.
11. 경골신경 단면(직경 3–5mm) 내에서 3-phase TIS의 조향 분해능(resolution)을 추정하는 방법을 제안하라.
